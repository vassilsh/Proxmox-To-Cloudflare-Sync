services:
  proxmox-cloudflare-sync:
    build: .
    container_name: proxmox-cloudflare-sync
    restart: unless-stopped
    env_file:
      - .env  # Keep all your secret tokens here
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"